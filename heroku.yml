setup:
    addons:
      - plan: heroku-postgresql
        as: DATABASE
      - plan: heroku-redis:mini

build:
    docker:
        web: Dockerfile
        worker: Dockerfile
        webhook: Dockerfile